{{- $duckdb := .Values.fabric.engines.duckdb }}
{{- $catalog := .Values.fabric.catalog | default dict }}
{{- if and .Values.fabric.enabled $catalog.enabled }}
{{- if not .Values.externalSecrets.enabled }}
{{- fail "externalSecrets.enabled must be true when fabric.catalog.enabled is true" }}
{{- end }}
{{- $secret := $catalog.secret | default dict }}
{{- $store := required "fabric.catalog.secret.store is required when the catalog is enabled" $secret.store }}
{{- if not (get (.Values.externalSecrets.stores | default dict) $store) }}
{{- fail (printf "fabric.catalog.secret.store %q is not defined in externalSecrets.stores" $store) }}
{{- end }}
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ .Values.app.name }}-catalog
  labels:
    app.kubernetes.io/name: {{ .Values.app.name }}
    app.kubernetes.io/version: {{ .Release.Name }}-{{ .Release.Revision }}
    app: {{ .Values.app.name }}
    group: {{ .Values.app.group }}
spec:
  refreshInterval: 30s
  secretStoreRef:
    name: {{ .Values.app.name }}-aws-secretstore-{{ $store }}
    kind: SecretStore
  target:
    name: {{ .Values.app.name }}-catalog
    creationPolicy: Owner
    template:
      type: Opaque
      data:
        token: "{{ `{{ .token }}` }}"
  data:
    - secretKey: token
      remoteRef:
        key: {{ required "fabric.catalog.secret.key is required when the catalog is enabled" $secret.key }}
        property: {{ required "fabric.catalog.secret.property is required when the catalog is enabled" $secret.property }}
{{- end }}
