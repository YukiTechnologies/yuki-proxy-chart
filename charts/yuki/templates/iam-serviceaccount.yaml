{{- if and .Values.serviceAccount.serviceAccountName .Values.serviceAccount.roleARN }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount.serviceAccountName }}
  annotations:
    eks.amazonaws.com/role-arn: {{ .Values.serviceAccount.roleARN }}
{{- end }}