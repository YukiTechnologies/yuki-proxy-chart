apiVersion: v1
kind: Secret
metadata:
  name: redis-encryption-key
type: Opaque
data:
{{- if and .Values.redisSecret.create (not .Release.IsUpgrade) }}
  password: {{ randAlphaNum 32 | b64enc | quote }}
{{- else }}
  password: {{ .Values.redisSecret.create | quote }}
{{- end }}
